spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
          - namespace: dafault
            name: config-example
      reload:
        enabled: true
        mode: event
        strategy: refresh