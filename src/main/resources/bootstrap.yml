spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        name: config-example
        namepsace: default
        sources:
          - namespace: default
            name: config-example
      reload:
        enabled: true
        mode: event
        strategy: refresh