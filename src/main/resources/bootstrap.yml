spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        enableApi: true
        name: config-example
        namespace: default
        sources:
          - namespace: default
            name: config-example
      reload:
        enabled: true
        mode: polling
        strategy: refresh


